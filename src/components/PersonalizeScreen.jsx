/**
 * PersonalizeScreen (deprecated)
 *
 * Esta vista de personalizacion fue reemplazada por el modal "Personalizar"
 * que vive dentro de <AvatarPlayer />. Se conserva el archivo como stub para
 * no romper imports antiguos en caso de revertir un commit, pero ya no se
 * referencia desde la app.
 */
export default function PersonalizeScreen() {
  return null
}
